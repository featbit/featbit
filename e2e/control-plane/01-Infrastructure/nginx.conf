worker_processes 1;

events {
    worker_connections 1024;
}

http {
    include mime.types;
    default_type application/octet-stream;
    sendfile on;
    keepalive_timeout 65;

    # Access log format that includes which upstream backend handled each request.
    # $upstream_addr shows the backend IP:port (e.g. 127.0.0.1:8081 = west UI,
    # 127.0.0.1:8082 = east UI, 127.0.0.1:15000 = west API, etc.)
    log_format upstream '$remote_addr - $remote_user [$time_local] '
                        '"$request" $status $body_bytes_sent '
                        'upstream=$upstream_addr rt=$request_time';
    access_log logs/access.log upstream;
    
    # Increase hash bucket size for long server names
    server_names_hash_bucket_size 255;
    map_hash_bucket_size 64;

    # ── Dynamic CORS origin — echoes back any known FeatBit origin ───────────
    # Allows the load-balanced featbit.local origin to reach per-cluster API/eval
    # endpoints (featbit-api.west.local, featbit-api.east.local, etc.) without
    # hardcoding a single allowed origin in each server block.
    map $http_origin $cors_origin {
        default                      "";
        "http://featbit.local"        "http://featbit.local";
        "http://featbit.west.local"   "http://featbit.west.local";
        "http://featbit.east.local"   "http://featbit.east.local";
    }

    # ── Active/Active upstreams (round-robin across west + east) ──────────────
    upstream featbit_ui {
        server 127.0.0.1:8081;
        server 127.0.0.1:8082;
    }

    upstream featbit_api {
        server 127.0.0.1:15000;
        server 127.0.0.1:15001;
    }

    upstream featbit_eval {
        # cp09 multi-replica topology: each cluster runs 3 eval-server pods,
        # each port-forwarded to a dedicated host slot. nginx round-robins
        # across all active slots so a single client's connection lands on
        # one specific pod (proven via Redis-side scan of featbit:connection:*).
        # When a pod dies its slot's port-forward dies → nginx marks the slot
        # down (max_fails=2 fail_timeout=2s) → new connections route to
        # surviving slots. When the replacement pod's port-forward comes back
        # nginx re-probes within 2s.
        #
        # West slots: 5100/5102/5104 active, 5106/5108 spare for scale-up.
        # East slots: 5101/5103/5105 active, 5107/5109 spare for scale-up.
        # Spares are marked `backup` so nginx only routes to them when ALL
        # primary slots in their cluster are down (used during scale-up
        # without forcing rotation churn).
        server 127.0.0.1:5100 max_fails=2 fail_timeout=2s;
        server 127.0.0.1:5102 max_fails=2 fail_timeout=2s;
        server 127.0.0.1:5104 max_fails=2 fail_timeout=2s;
        server 127.0.0.1:5101 max_fails=2 fail_timeout=2s;
        server 127.0.0.1:5103 max_fails=2 fail_timeout=2s;
        server 127.0.0.1:5105 max_fails=2 fail_timeout=2s;
        server 127.0.0.1:5106 max_fails=2 fail_timeout=2s backup;
        server 127.0.0.1:5108 max_fails=2 fail_timeout=2s backup;
        server 127.0.0.1:5107 max_fails=2 fail_timeout=2s backup;
        server 127.0.0.1:5109 max_fails=2 fail_timeout=2s backup;
    }

    # ── Active/Active load-balanced servers ───────────────────────────────────

    # Load-balanced UI
    server {
        listen 80;
        server_name featbit.local;

        # Handle the bare-root redirect here so it never reaches the upstream.
        # The UI container redirects / → /en internally (via rewrite ^/$), but
        # proxy_intercept_errors on intercepts that 301 and its behaviour with an
        # upstream group is unreliable.  Doing the redirect ourselves is simpler
        # and avoids any round-robin ambiguity.
        location = / {
            return 302 /en;
        }

        location / {
            proxy_pass http://featbit_ui;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;

            proxy_intercept_errors on;
            error_page 404 = @lb_spa_fallback;
        }

        location @lb_spa_fallback {
            rewrite ^ /index.html break;
            proxy_pass http://featbit_ui;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
    }

    # Load-balanced API
    server {
        listen 80;
        server_name featbit-api.local;

        location / {
            proxy_pass http://featbit_api;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;

            # Hide backend CORS headers to avoid duplicates
            proxy_hide_header Access-Control-Allow-Origin;
            proxy_hide_header Access-Control-Allow-Methods;
            proxy_hide_header Access-Control-Allow-Headers;
            proxy_hide_header Access-Control-Allow-Credentials;
            proxy_hide_header Access-Control-Max-Age;

            # CORS headers - allow any known FeatBit origin
            add_header Access-Control-Allow-Origin $cors_origin always;
            add_header Access-Control-Allow-Methods "GET, POST, PUT, DELETE, OPTIONS, PATCH" always;
            add_header Access-Control-Allow-Headers "DNT,Keep-Alive,User-Agent,X-Requested-With,If-Modified-Since,Cache-Control,Content-Type,Range,Authorization,organization,workspace,accept,accept-language" always;
            add_header Access-Control-Allow-Credentials "true" always;
            add_header Access-Control-Max-Age "3600" always;

            if ($request_method = OPTIONS) {
                return 204;
            }
        }
    }

    # Load-balanced Evaluation (WebSocket-aware)
    server {
        listen 80;
        server_name featbit-eval.local;

        location / {
            proxy_pass http://featbit_eval;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;

            # WebSocket upgrade (required for the evaluation-server /streaming endpoint)
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "Upgrade";
            proxy_read_timeout 86400;

            # When an upstream slot is down (pod killed, port-forward not yet
            # re-bound), nginx fails over to the next slot in featbit_eval
            # immediately rather than returning 502 to the client. Required for
            # cp09 intra-cluster and cross-cluster failover assertions.
            proxy_next_upstream error timeout http_502 http_503 http_504;
            proxy_next_upstream_tries 5;
            proxy_next_upstream_timeout 5s;

            # Hide backend CORS headers to avoid duplicates
            proxy_hide_header Access-Control-Allow-Origin;
            proxy_hide_header Access-Control-Allow-Methods;
            proxy_hide_header Access-Control-Allow-Headers;
            proxy_hide_header Access-Control-Allow-Credentials;
            proxy_hide_header Access-Control-Max-Age;

            # CORS headers - allow any known FeatBit origin
            add_header Access-Control-Allow-Origin $cors_origin always;
            add_header Access-Control-Allow-Methods "GET, POST, PUT, DELETE, OPTIONS, PATCH" always;
            add_header Access-Control-Allow-Headers "DNT,Keep-Alive,User-Agent,X-Requested-With,If-Modified-Since,Cache-Control,Content-Type,Range,Authorization,organization,workspace,accept,accept-language" always;
            add_header Access-Control-Allow-Credentials "true" always;
            add_header Access-Control-Max-Age "3600" always;

            if ($request_method = OPTIONS) {
                return 204;
            }
        }
    }

    # ── Per-cluster servers ───────────────────────────────────────────────────

    # West UI
    server {
        listen 80;
        server_name featbit.west.local;

        location / {
            proxy_pass http://127.0.0.1:8081;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;

            # Angular SPA fallback: intercept upstream 404s and serve index.html
            # so direct deep-link navigation (e.g. /login, /feature-flags) works.
            proxy_intercept_errors on;
            error_page 404 = @west_spa_fallback;
        }

        location @west_spa_fallback {
            rewrite ^ /index.html break;
            proxy_pass http://127.0.0.1:8081;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
    }

    # West API
    server {
        listen 80;
        server_name featbit-api.west.local;

        location / {
            proxy_pass http://127.0.0.1:15000;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            
            # Hide backend CORS headers to avoid duplicates
            proxy_hide_header Access-Control-Allow-Origin;
            proxy_hide_header Access-Control-Allow-Methods;
            proxy_hide_header Access-Control-Allow-Headers;
            proxy_hide_header Access-Control-Allow-Credentials;
            proxy_hide_header Access-Control-Max-Age;
            
            # CORS headers - allow any known FeatBit origin
            add_header Access-Control-Allow-Origin $cors_origin always;
            add_header Access-Control-Allow-Methods "GET, POST, PUT, DELETE, OPTIONS, PATCH" always;
            add_header Access-Control-Allow-Headers "DNT,Keep-Alive,User-Agent,X-Requested-With,If-Modified-Since,Cache-Control,Content-Type,Range,Authorization,organization,workspace,accept,accept-language" always;
            add_header Access-Control-Allow-Credentials "true" always;
            add_header Access-Control-Max-Age "3600" always;
            
            if ($request_method = OPTIONS) {
                return 204;
            }
        }
    }

    # West Evaluation
    server {
        listen 80;
        server_name featbit-eval.west.local;

        location / {
            proxy_pass http://127.0.0.1:5100;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;

            # WebSocket upgrade (required for the evaluation-server /streaming endpoint)
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "Upgrade";
            proxy_read_timeout 86400;

            # Hide backend CORS headers to avoid duplicates
            proxy_hide_header Access-Control-Allow-Origin;
            proxy_hide_header Access-Control-Allow-Methods;
            proxy_hide_header Access-Control-Allow-Headers;
            proxy_hide_header Access-Control-Allow-Credentials;
            proxy_hide_header Access-Control-Max-Age;
            
            # CORS headers - allow any known FeatBit origin
            add_header Access-Control-Allow-Origin $cors_origin always;
            add_header Access-Control-Allow-Methods "GET, POST, PUT, DELETE, OPTIONS, PATCH" always;
            add_header Access-Control-Allow-Headers "DNT,Keep-Alive,User-Agent,X-Requested-With,If-Modified-Since,Cache-Control,Content-Type,Range,Authorization,organization,workspace,accept,accept-language" always;
            add_header Access-Control-Allow-Credentials "true" always;
            add_header Access-Control-Max-Age "3600" always;
            
            if ($request_method = OPTIONS) {
                return 204;
            }
        }
    }

    # East UI
    server {
        listen 80;
        server_name featbit.east.local;

        location / {
            proxy_pass http://127.0.0.1:8082;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;

            # Angular SPA fallback: intercept upstream 404s and serve index.html
            # so direct deep-link navigation (e.g. /login, /feature-flags) works.
            proxy_intercept_errors on;
            error_page 404 = @east_spa_fallback;
        }

        location @east_spa_fallback {
            rewrite ^ /index.html break;
            proxy_pass http://127.0.0.1:8082;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
    }

    # East API
    server {
        listen 80;
        server_name featbit-api.east.local;

        location / {
            proxy_pass http://127.0.0.1:15001;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            
            # Hide backend CORS headers to avoid duplicates
            proxy_hide_header Access-Control-Allow-Origin;
            proxy_hide_header Access-Control-Allow-Methods;
            proxy_hide_header Access-Control-Allow-Headers;
            proxy_hide_header Access-Control-Allow-Credentials;
            proxy_hide_header Access-Control-Max-Age;
            
            # CORS headers - allow any known FeatBit origin
            add_header Access-Control-Allow-Origin $cors_origin always;
            add_header Access-Control-Allow-Methods "GET, POST, PUT, DELETE, OPTIONS, PATCH" always;
            add_header Access-Control-Allow-Headers "DNT,Keep-Alive,User-Agent,X-Requested-With,If-Modified-Since,Cache-Control,Content-Type,Range,Authorization,organization,workspace,accept,accept-language" always;
            add_header Access-Control-Allow-Credentials "true" always;
            add_header Access-Control-Max-Age "3600" always;
            
            if ($request_method = OPTIONS) {
                return 204;
            }
        }
    }

    # West Kafka UI
    server {
        listen 80;
        server_name featbit-kafka.west.local;

        location / {
            proxy_pass http://127.0.0.1:18080;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
    }

    # West Control Plane
    server {
        listen 80;
        server_name featbit-control-plane.west.local;

        location / {
            proxy_pass http://127.0.0.1:5200;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;

            # Hide backend CORS headers to avoid duplicates
            proxy_hide_header Access-Control-Allow-Origin;
            proxy_hide_header Access-Control-Allow-Methods;
            proxy_hide_header Access-Control-Allow-Headers;
            proxy_hide_header Access-Control-Allow-Credentials;
            proxy_hide_header Access-Control-Max-Age;

            # CORS headers - allow everything for development
            add_header Access-Control-Allow-Origin "*" always;
            add_header Access-Control-Allow-Methods "GET, POST, PUT, DELETE, OPTIONS, PATCH" always;
            add_header Access-Control-Allow-Headers "DNT,Keep-Alive,User-Agent,X-Requested-With,If-Modified-Since,Cache-Control,Content-Type,Range,Authorization,X-API-Key,accept,accept-language" always;
            add_header Access-Control-Max-Age "3600" always;

            if ($request_method = OPTIONS) {
                return 204;
            }
        }
    }

    # East Control Plane
    server {
        listen 80;
        server_name featbit-control-plane.east.local;

        location / {
            proxy_pass http://127.0.0.1:5201;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;

            # Hide backend CORS headers to avoid duplicates
            proxy_hide_header Access-Control-Allow-Origin;
            proxy_hide_header Access-Control-Allow-Methods;
            proxy_hide_header Access-Control-Allow-Headers;
            proxy_hide_header Access-Control-Allow-Credentials;
            proxy_hide_header Access-Control-Max-Age;

            # CORS headers - allow everything for development
            add_header Access-Control-Allow-Origin "*" always;
            add_header Access-Control-Allow-Methods "GET, POST, PUT, DELETE, OPTIONS, PATCH" always;
            add_header Access-Control-Allow-Headers "DNT,Keep-Alive,User-Agent,X-Requested-With,If-Modified-Since,Cache-Control,Content-Type,Range,Authorization,X-API-Key,accept,accept-language" always;
            add_header Access-Control-Max-Age "3600" always;

            if ($request_method = OPTIONS) {
                return 204;
            }
        }
    }

    # East Kafka UI
    server {
        listen 80;
        server_name featbit-kafka.east.local;

        location / {
            proxy_pass http://127.0.0.1:18081;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
    }

    # East Evaluation
    server {
        listen 80;
        server_name featbit-eval.east.local;

        location / {
            proxy_pass http://127.0.0.1:5101;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;

            # WebSocket upgrade (required for the evaluation-server /streaming endpoint)
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "Upgrade";
            proxy_read_timeout 86400;

            # Hide backend CORS headers to avoid duplicates
            proxy_hide_header Access-Control-Allow-Origin;
            proxy_hide_header Access-Control-Allow-Methods;
            proxy_hide_header Access-Control-Allow-Headers;
            proxy_hide_header Access-Control-Allow-Credentials;
            proxy_hide_header Access-Control-Max-Age;
            
            # CORS headers - allow any known FeatBit origin
            add_header Access-Control-Allow-Origin $cors_origin always;
            add_header Access-Control-Allow-Methods "GET, POST, PUT, DELETE, OPTIONS, PATCH" always;
            add_header Access-Control-Allow-Headers "DNT,Keep-Alive,User-Agent,X-Requested-With,If-Modified-Since,Cache-Control,Content-Type,Range,Authorization,organization,workspace,accept,accept-language" always;
            add_header Access-Control-Allow-Credentials "true" always;
            add_header Access-Control-Max-Age "3600" always;
            
            if ($request_method = OPTIONS) {
                return 204;
            }
        }
    }
}
