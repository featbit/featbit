# Basic settings for running in production. Change accordingly before deploying the server.

# Database

# The database vendor.
db = postgres

# The username of the database user.
db-username = bn_keycloak

# The password of the database user.
db-password = 

# The full database JDBC URL. If not provided, a default URL is set based on the selected database vendor.
db-url = jdbc:postgresql://postgresql:5432/bitnami_keycloak?currentSchema=public

# Observability

# If the server should expose healthcheck endpoints.
health-enabled=true

# If the server should expose metrics endpoints.
metrics-enabled = false

# HTTP

# The file path to a server certificate or certificate chain in PEM format.
https-certificate-file = /opt/bitnami/keycloak/certs/featbit-example3.crt

# The file path to a private key in PEM format.
https-certificate-key-file = /opt/bitnami/keycloak/certs/featbit-example3-pkcs-key.pem

# The proxy address forwarding mode if the server is behind a reverse proxy.
proxy = passthrough

# Do not attach route to cookies and rely on the session affinity capabilities from reverse proxy
#spi-sticky-session-encoder-infinispan-should-attach-route=false

# Hostname for the Keycloak server.
hostname=keycloak.featbit.example
http-enabled = true
http-relative-path = /
http-port = 8080
https-port = 8443
hostname-strict = false
cache = ispn
log-level = info
log-console-output = default
