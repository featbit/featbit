<?xml version="1.0"?>
<doc>
    <assembly>
        <name>Infrastructure</name>
    </assembly>
    <members>
        <member name="T:Infrastructure.AppService.StagedFlagGcWorker">
             <summary>
             Garbage-collects superseded, immutable versioned flag value keys written by the B1 stage/commit
             storage (<c>featbit:flag:{id}:v{ts}</c>).
            
             Periodically scans the versioned key-space via Redis <c>SCAN</c> (never <c>KEYS</c>), groups keys
             by flag id, reads each flag's committed pointer (<c>featbit:flag-committed:{id}</c>), and deletes
             only versions whose <c>ts</c> is strictly LESS THAN the committed <c>ts</c>. The currently
             committed version, and any newer (staged-future) version with <c>ts &gt;= committed</c>, is never
             deleted. A flag with no committed pointer is skipped entirely so unstaged/uncommitted versions
             are left untouched.
            
             The worker only runs under <see cref="F:Application.ControlPlane.ConsistencyMode.GatedCommit"/>; otherwise it no-ops.
             </summary>
        </member>
        <member name="F:Infrastructure.AppService.StagedFlagGcWorker.DefaultInterval">
            <summary>
            Default interval between GC sweeps when not overridden via
            <c>ControlPlane:StagedFlagGc:IntervalSeconds</c>.
            </summary>
        </member>
        <member name="M:Infrastructure.AppService.StagedFlagGcWorker.RunGcOnceAsync(System.Threading.CancellationToken)">
             <summary>
             Performs a single GC sweep over all versioned flag value keys and returns the number of keys
             deleted. Exposed so it can be invoked directly (e.g. by integration tests) without waiting on
             the periodic timer.
            
             For each flag: if it has no committed pointer the flag is skipped; otherwise every versioned
             key with <c>ts &lt; committed</c> is deleted. The committed version and any version with
             <c>ts &gt;= committed</c> are preserved.
             </summary>
        </member>
        <member name="M:Infrastructure.AppService.StagedFlagGcWorker.TryParseVersionedKey(StackExchange.Redis.RedisKey,System.Guid@,System.Int64@)">
            <summary>
            Parses a versioned flag value key of the form <c>featbit:flag:{guid}:v{ts}</c>. The committed
            pointer key (<c>featbit:flag-committed:{id}</c>) and the bare value key
            (<c>featbit:flag:{id}</c>) do not match this shape and are rejected.
            </summary>
        </member>
        <member name="M:Infrastructure.Caches.Redis.RedisCaches.FlagStaged(Domain.FeatureFlags.FeatureFlag,System.Int64)">
            <summary>
            Builds the versioned staged value entry for <paramref name="flag"/> scored by
            <paramref name="ts"/>. Mirrors <see cref="M:Infrastructure.Caches.Redis.RedisCaches.Flag(Domain.FeatureFlags.FeatureFlag)"/> serialization.
            </summary>
        </member>
        <member name="M:Infrastructure.Caches.Redis.RedisCaches.SegmentStaged(Domain.Segments.Segment,System.Int64)">
            <summary>
            Builds the versioned staged value entry for <paramref name="segment"/> scored by
            <paramref name="ts"/>. Mirrors <see cref="M:Infrastructure.Caches.Redis.RedisCaches.Segment(Domain.Segments.Segment)"/> serialization.
            </summary>
        </member>
        <member name="F:Infrastructure.Caches.Redis.RedisCacheService.OnlyAdvancePointerScript">
            <summary>
            Only-advance compare-and-set for a single string pointer key: KEYS[1] is set to ARGV[1] only
            if the key does not yet exist or its current value is strictly less than ARGV[1]. Backs the
            committed-pointer flip in <see cref="M:Infrastructure.Caches.Redis.RedisCacheService.CommitFlagAsync(System.Guid,System.String,System.Int64)"/>/<see cref="M:Infrastructure.Caches.Redis.RedisCacheService.CommitSegmentAsync(System.Collections.Generic.ICollection{System.Guid},System.String,System.Int64)"/>.
            </summary>
        </member>
        <member name="F:Infrastructure.Caches.Redis.RedisCacheService.OnlyAdvanceIndexedUpsertScript">
            <summary>
            Only-advance guarded upsert keyed off one or more sorted-set index scores (the version
            register every normal upsert/commit already maintains as UpdatedAt-unix-ms): KEYS[1] is the
            value key, KEYS[2..N] are the index keys (a flag has exactly one; a segment can have one per
            env it belongs to). Two decisions, evaluated independently in the same atomic EVAL:
            <list type="bullet">
            <item>The value key is written iff the new score ARGV[3] is strictly greater than the
            register — the MAX of ZSCORE(KEYS[i], ARGV[2]) across ALL of KEYS[2..N] (a missing
            member/key contributes nothing, i.e. is treated as absent/-inf, never as 0). So the value
            key can only ever be overwritten with content that is strictly newer than EVERY env's
            current index score; a single lagging/desynced index is enough to block it, exactly as a
            single fresher env should.</item>
            <item>EVERY index key still gets an unconditional <c>ZADD ... GT</c> for ARGV[3]/ARGV[2],
            regardless of whether the value key ended up written. GT is a per-key only-advance
            primitive: an index already at or ahead of ARGV[3] is left untouched (so no index can ever
            move backward, and none can be dragged past what its own history supports), while a
            lagging index that is behind ARGV[3] still catches up — even on a call where the value key
            write was blocked by a *different*, fresher env. This lets desynced indexes re-converge
            over time without ever letting an index overstate freshness beyond the true committed
            register (GT can only advance a lagging index up to ARGV[3], and ARGV[3] was, by
            construction, not fresher than the value actually on disk when the value write was
            blocked).</item>
            </list>
            Returns 1 iff the value key was written, 0 otherwise. Backs the targeted BestEffort legacy
            upserts used by the backfiller (<see cref="M:Infrastructure.Caches.Redis.RedisCacheService.UpsertFlagIfNewerAsync(Domain.FeatureFlags.FeatureFlag)"/> passes a single index
            key; <see cref="M:Infrastructure.Caches.Redis.RedisCacheService.UpsertSegmentIfNewerAsync(System.Collections.Generic.ICollection{System.Guid},Domain.Segments.Segment)"/> passes one per env).
            </summary>
        </member>
        <member name="M:Infrastructure.Caches.Redis.RedisCacheService.UpsertFlagIfNewerAsync(Domain.FeatureFlags.FeatureFlag)">
            <summary>
            Only-advance targeted upsert (#89): behaves like <see cref="M:Infrastructure.Caches.Redis.RedisCacheService.UpsertFlagAsync(Domain.FeatureFlags.FeatureFlag)"/> but guarded by
            the env flag index score (already the UpdatedAt-unix-ms version register every normal upsert
            maintains) so a stale write can never revert a fresher legacy value + index entry. This is
            used ONLY by the backfiller's targeted per-DC writes
            (<see cref="T:Application.Caches.ICacheService"/> callers via the composite cache's
            UpsertFlagToDcAsync) — the normal broadcast <see cref="M:Infrastructure.Caches.Redis.RedisCacheService.UpsertFlagAsync(Domain.FeatureFlags.FeatureFlag)"/> stays unconditional.
            </summary>
        </member>
        <member name="M:Infrastructure.Caches.Redis.RedisCacheService.StageFlagAsync(Domain.FeatureFlags.FeatureFlag,System.Int64)">
            <summary>
            Stages a new flag version (B1 stage/commit storage). Writes ONLY the versioned value key
            <c>featbit:flag:{id}:v{ts}</c>; it does NOT move the committed pointer and does NOT touch
            the env flag sorted-set index. The previously committed value therefore stays readable
            until <see cref="M:Infrastructure.Caches.Redis.RedisCacheService.CommitFlagAsync(System.Guid,System.String,System.Int64)"/> is called.
            </summary>
        </member>
        <member name="M:Infrastructure.Caches.Redis.RedisCacheService.CommitFlagAsync(System.Guid,System.String,System.Int64)">
             <summary>
             Commits a previously staged flag version (B1 stage/commit storage). Sets the committed
             pointer <c>featbit:flag-committed:{id}</c> to <paramref name="ts"/> AND advances the env
             flag sorted-set index score to <paramref name="ts"/> (mirroring <see cref="M:Infrastructure.Caches.Redis.RedisCacheService.UpsertFlagAsync(Domain.FeatureFlags.FeatureFlag)"/>'s
             index logic), making the staged version the authoritative committed value.
            
             Only-advance guarded (#89): both the pointer flip and the index score use only-advance
             semantics, so a call with a <paramref name="ts"/> that is not strictly newer than what's
             already committed is a no-op — this is what stops a DC cache backfill holding a stale
             snapshot from reverting a fresher pointer a racing normal commit already wrote. The normal
             coordinator commit path always calls this with a strictly increasing <paramref name="ts"/>
             (enforced by its own monotonicity check before broadcasting), so the guard never fires there.
             </summary>
        </member>
        <member name="M:Infrastructure.Caches.Redis.RedisCacheService.HasStagedFlagAsync(System.Guid,System.Int64)">
            <summary>
            Probes whether this Redis holds the staged version value key
            <c>featbit:flag:{id}:v{ts}</c> (B1 stage/commit storage).
            </summary>
        </member>
        <member name="M:Infrastructure.Caches.Redis.RedisCacheService.UpsertSegmentIfNewerAsync(System.Collections.Generic.ICollection{System.Guid},Domain.Segments.Segment)">
            <summary>
            Only-advance targeted upsert (#89, segment counterpart of <see cref="M:Infrastructure.Caches.Redis.RedisCacheService.UpsertFlagIfNewerAsync(Domain.FeatureFlags.FeatureFlag)"/>):
            behaves like <see cref="M:Infrastructure.Caches.Redis.RedisCacheService.UpsertSegmentAsync(System.Collections.Generic.ICollection{System.Guid},Domain.Segments.Segment)"/> but guarded by the MAX score across EVERY env's
            segment index as the version register (see <see cref="F:Infrastructure.Caches.Redis.RedisCacheService.OnlyAdvanceIndexedUpsertScript"/> for the
            full invariant), so the value key can never be reverted by a write that isn't strictly newer
            than what ANY of the segment's envs already has, while each env's own index is still free to
            independently catch up (per-key GT) even on a call whose value write a *different*, fresher env
            blocked. Used ONLY by the backfiller's targeted per-DC writes; the normal broadcast
            <see cref="M:Infrastructure.Caches.Redis.RedisCacheService.UpsertSegmentAsync(System.Collections.Generic.ICollection{System.Guid},Domain.Segments.Segment)"/> stays unconditional. If <paramref name="envIds"/> is empty
            there is no index to use as a version register, so this falls back to the unconditional write
            (matching pre-#89 behavior for that edge case).
            </summary>
        </member>
        <member name="M:Infrastructure.Caches.Redis.RedisCacheService.StageSegmentAsync(Domain.Segments.Segment,System.Int64)">
            <summary>
            Stages a new segment version (B2 stage/commit storage, mirroring <see cref="M:Infrastructure.Caches.Redis.RedisCacheService.StageFlagAsync(Domain.FeatureFlags.FeatureFlag,System.Int64)"/>).
            Writes ONLY the versioned value key <c>featbit:segment:{id}:v{ts}</c>; it does NOT move the
            committed pointer and does NOT touch any env segment sorted-set index. The previously
            committed value therefore stays readable until <see cref="M:Infrastructure.Caches.Redis.RedisCacheService.CommitSegmentAsync(System.Collections.Generic.ICollection{System.Guid},System.String,System.Int64)"/> is called.
            </summary>
        </member>
        <member name="M:Infrastructure.Caches.Redis.RedisCacheService.CommitSegmentAsync(System.Collections.Generic.ICollection{System.Guid},System.String,System.Int64)">
             <summary>
             Commits a previously staged segment version (B2 stage/commit storage, mirroring
             <see cref="M:Infrastructure.Caches.Redis.RedisCacheService.CommitFlagAsync(System.Guid,System.String,System.Int64)"/>). Sets the committed pointer
             <c>featbit:segment-committed:{id}</c> to <paramref name="ts"/> AND advances the segment
             sorted-set index score to <paramref name="ts"/> for each env in <paramref name="envIds"/>
             (segments can belong to multiple envs, mirroring <see cref="M:Infrastructure.Caches.Redis.RedisCacheService.UpsertSegmentAsync(System.Collections.Generic.ICollection{System.Guid},Domain.Segments.Segment)"/>'s index
             logic), making the staged version the authoritative committed value.
            
             Only-advance guarded (#89, mirrors <see cref="M:Infrastructure.Caches.Redis.RedisCacheService.CommitFlagAsync(System.Guid,System.String,System.Int64)"/>): both the pointer flip and
             every env's index score use only-advance semantics, so a stale <paramref name="ts"/> is a
             no-op and can never revert a fresher committed segment pointer. The normal coordinator commit
             path always advances <paramref name="ts"/>, so the guard never fires there.
             </summary>
        </member>
        <member name="M:Infrastructure.Caches.Redis.RedisCacheService.HasStagedSegmentAsync(System.Guid,System.Int64)">
            <summary>
            Probes whether this Redis holds the staged version value key
            <c>featbit:segment:{id}:v{ts}</c> (B2 stage/commit storage, mirroring
            <see cref="M:Infrastructure.Caches.Redis.RedisCacheService.HasStagedFlagAsync(System.Guid,System.Int64)"/>).
            </summary>
        </member>
        <member name="F:Infrastructure.Caches.Redis.RedisClient.DefaultRedisTimeoutMs">
            <summary>
            Default StackExchange.Redis <c>connectTimeout</c>/<c>syncTimeout</c> (ms) applied by
            <see cref="M:Infrastructure.Caches.Redis.RedisClient.EnsureSelfHealingOptions(System.String,System.Int32,System.Int32)"/> (#106) when neither option is already present in the
            connection string, and not overridden via <c>Redis:ConnectTimeoutMs</c> /
            <c>Redis:SyncTimeoutMs</c> (only consulted by the <see cref="M:Infrastructure.Caches.Redis.RedisClient.#ctor(Microsoft.Extensions.Configuration.IConfiguration)"/>
            constructor). Mirrors the control-plane default
            (<c>CacheServiceCollectionExtensions.DefaultRedisTimeoutMs</c>, #92): 1500ms bounds
            StackExchange.Redis's own (much longer) default wait for an unreachable/down Redis, without
            being so tight that normal load trips false-positive command failures.
            </summary>
        </member>
        <member name="M:Infrastructure.Caches.Redis.RedisClient.EnsureSelfHealingOptions(System.String,System.Int32,System.Int32)">
            <summary>
            #106: makes the multiplexer self-healing. Without <c>abortConnect=false</c>, Redis being
            unreachable at first touch (common during rolling deploys) makes
            <c>ConnectionMultiplexer.Connect</c> throw, and the <see cref="T:System.Lazy`1"/>
            above caches that exception PERMANENTLY — every later access re-throws until the pod
            restarts, even after Redis recovers. With <c>abortConnect=false</c>, <c>Connect</c> returns
            immediately and reconnects in the background instead. Also appends explicit
            <c>connectTimeout</c>/<c>syncTimeout</c> so a command issued while disconnected fails fast
            instead of waiting StackExchange.Redis's own (much longer) default. Ported from the
            control-plane template (<c>CacheServiceCollectionExtensions.AppendRedisTimeouts</c>, #92) —
            only appends an option when the operator hasn't already specified it (case-insensitive token
            match), so an explicit override always wins. Safe for Sentinel-style / multi-host connection
            strings since StackExchange.Redis recognizes an option token anywhere after the first
            endpoint. Internal + <c>InternalsVisibleTo</c> so this is unit-testable without a real Redis.
            </summary>
        </member>
        <member name="M:Infrastructure.Caches.Redis.RedisKeys.FlagCommittedPointer(System.Guid)">
            <summary>
            The committed-pointer key holding the timestamp of the currently committed flag version:
            <c>featbit:flag-committed:{id}</c>.
            </summary>
        </member>
        <member name="M:Infrastructure.Caches.Redis.RedisKeys.FlagVersion(System.Guid,System.Int64)">
            <summary>
            The versioned, immutable value key for a single staged flag version: <c>featbit:flag:{id}:v{ts}</c>.
            </summary>
        </member>
        <member name="F:Infrastructure.Caches.Redis.RedisKeys.VersionedFlagKeyPattern">
            <summary>
            SCAN match pattern selecting only versioned flag value keys.
            </summary>
        </member>
        <member name="M:Infrastructure.Caches.Redis.RedisKeys.SegmentCommittedPointer(System.Guid)">
            <summary>
            The committed-pointer key holding the timestamp of the currently committed segment version:
            <c>featbit:segment-committed:{id}</c>.
            </summary>
        </member>
        <member name="M:Infrastructure.Caches.Redis.RedisKeys.SegmentVersion(System.Guid,System.Int64)">
            <summary>
            The versioned, immutable value key for a single staged segment version:
            <c>featbit:segment:{id}:v{ts}</c>.
            </summary>
        </member>
        <member name="P:Infrastructure.Caches.Redis.RedisOptions.PopulateLockTtlSeconds">
            <summary>
            Lock TTL (seconds) used by <see cref="T:Infrastructure.Caches.Redis.RedisPopulatingService"/> when taking the
            <c>featbit:populate-redis</c> lock at startup. The lock prevents multiple back-end
            instances from running the Redis populate concurrently. Must be set to a value greater
            than the expected populate duration; if the lock expires before the populate finishes,
            another instance can acquire the lock and run a concurrent populate. Default is
            60 seconds, which is sufficient for most deployments. Increase this value
            if your dataset is large enough that populate regularly takes longer than 60 seconds.
            </summary>
        </member>
        <member name="P:Infrastructure.Caches.Redis.RedisOptions.PopulateMaxWaitSeconds">
            <summary>
            Maximum number of seconds an instance will wait for another instance to finish
            populating Redis before throwing. Instances that arrive while a populate is in progress
            poll the populate marker and re-attempt the lock; if the populating instance is stuck
            or crashed past this timeout, the waiting instance throws so the host can surface the
            failure (and the orchestrator can restart it) rather than serving against a partially
            populated Redis indefinitely. Default is 90 seconds (<see cref="P:Infrastructure.Caches.Redis.RedisOptions.PopulateLockTtlSeconds"/>
            plus a 30-second margin for lock expiry and polling overhead).
            </summary>
        </member>
        <member name="T:Infrastructure.Identity.JwtOptions">
            <summary>
            Strongly-typed representation of the <c>Jwt</c> configuration section.
            Supports three signing algorithms:
            <list type="bullet">
              <item><description><b>HS256</b> (default) — HMAC-SHA256 symmetric signing. Requires <see cref="P:Infrastructure.Identity.JwtOptions.Key"/>.</description></item>
              <item><description><b>RS256</b> — RSA-SHA256 asymmetric signing. Requires <see cref="P:Infrastructure.Identity.JwtOptions.PrivateKeyPath"/> and <see cref="P:Infrastructure.Identity.JwtOptions.PublicKeyPath"/>.</description></item>
              <item><description><b>ES256</b> — ECDSA-SHA256 asymmetric signing. Requires <see cref="P:Infrastructure.Identity.JwtOptions.PrivateKeyPath"/> and <see cref="P:Infrastructure.Identity.JwtOptions.PublicKeyPath"/>.</description></item>
            </list>
            </summary>
        </member>
        <member name="F:Infrastructure.Identity.JwtOptions.Jwt">
            <summary>
            Configuration section key used to bind this record from <c>appsettings.json</c>.
            </summary>
        </member>
        <member name="P:Infrastructure.Identity.JwtOptions.Algorithm">
            <summary>
            Signing algorithm. Valid values: <c>"HS256"</c> (default), <c>"RS256"</c>, or <c>"ES256"</c>.
            </summary>
        </member>
        <member name="P:Infrastructure.Identity.JwtOptions.Issuer">
            <summary>
            Expected issuer claim (<c>iss</c>) embedded in and validated from every JWT.
            </summary>
        </member>
        <member name="P:Infrastructure.Identity.JwtOptions.Audience">
            <summary>
            Expected audience claim (<c>aud</c>) embedded in and validated from every JWT.
            </summary>
        </member>
        <member name="P:Infrastructure.Identity.JwtOptions.Key">
            <summary>
            Symmetric shared secret used to sign and verify tokens when <see cref="P:Infrastructure.Identity.JwtOptions.Algorithm"/> is <c>"HS256"</c>.
            Not used for RS256 or ES256.
            </summary>
        </member>
        <member name="P:Infrastructure.Identity.JwtOptions.PrivateKeyPath">
            <summary>
            Absolute or relative path to the PEM-encoded <b>private</b> key file used to <em>sign</em> tokens
            when <see cref="P:Infrastructure.Identity.JwtOptions.Algorithm"/> is <c>"RS256"</c> or <c>"ES256"</c>. Not used for HS256.
            </summary>
        </member>
        <member name="P:Infrastructure.Identity.JwtOptions.PublicKeyPath">
            <summary>
            Absolute or relative path to the PEM-encoded <b>public</b> key file used to <em>verify</em> tokens
            when <see cref="P:Infrastructure.Identity.JwtOptions.Algorithm"/> is <c>"RS256"</c> or <c>"ES256"</c>. Not used for HS256.
            </summary>
        </member>
        <member name="P:Infrastructure.Identity.JwtOptions.SigningSecurityKey">
            <summary>
            The <see cref="T:Microsoft.IdentityModel.Tokens.SecurityKey"/> used to sign tokens when issuing tokens.
            </summary>
        </member>
        <member name="P:Infrastructure.Identity.JwtOptions.VerificationSecurityKey">
            <summary>
            The <see cref="T:Microsoft.IdentityModel.Tokens.SecurityKey"/> used to verify tokens when validating incoming tokens.
            </summary>
        </member>
        <member name="T:Infrastructure.MQ.Postgres.PostgresMessageConsumer.Log.__MessagePolledStruct">
            <summary> This API supports the logging infrastructure and is not intended to be used directly from your code. It is subject to change in the future. </summary>
        </member>
        <member name="T:Infrastructure.Services.EntityFrameworkCore.PendingOpRetryPolicy">
             <summary>
             Shared retry/backoff policy for the EF committed-vs-pending optimistic-concurrency loops in
             FeatureFlagService/SegmentService (SetPendingAsync/PromotePendingAsync). Consolidates the
             budget and the rationale in one place (#107, completing #108 cleanup item 6).
            
             Rationale for the budget: a racing writer that wins the row makes SaveChanges throw
             DbUpdateConcurrencyException (Postgres xmin token, #72/#76). Each conflict means another
             writer just committed ahead of us, so the version/monotonicity guards converge fast — a
             losing racer typically resolves within one or two retries. Under a synchronized ("lock-step")
             herd of racers that all read-then-write in the same instant, though, the original budget of 3
             retries could genuinely exhaust (~1-in-5 observed under stress) before the herd thinned out.
             Raised to 8 retries with jittered backoff between attempts to push exhaustion from a realistic
             event down to effectively unreachable, while keeping the worst-case wall-clock bounded: with
             backoff = Random(10, 50)ms * attemptNumber, the cumulative worst case across 8 attempts is
             sum(10..50 * n) for n in 1..8, i.e. roughly 0.4s-1.8s total.
            
             If the budget is ever exhausted anyway (pathological contention), the caller logs an ERROR
             with actionable context and rethrows — see FeatureFlagService/SegmentService SetPendingAsync.
             The residual edge (an orphaned Redis stage, GC'd by StagedFlagGc) is documented in
             e2e/control-plane/00-Docs/GATED-COMMIT-CONSISTENCY.md under Known limitations.
             </summary>
        </member>
        <member name="M:Infrastructure.Services.EntityFrameworkCore.PendingOpRetryPolicy.DelayAsync(System.Int32)">
            <summary>
            Jittered backoff before the next attempt. <paramref name="attemptNumber"/> is 1-based (the
            retry about to be made), so the very first backoff is never a zero-length delay.
            </summary>
        </member>
        <member name="T:Infrastructure.Services.EntityFrameworkCore.PostgresLeaseStore">
            <summary>
            Entity Framework Core / Postgres implementation of <see cref="T:Application.ControlPlane.ILeaseStore"/>.
            Backs the control-plane live-set and watermark tracking with the <c>dc_leases</c> table.
            </summary>
        </member>
        <member name="M:Infrastructure.Services.EntityFrameworkCore.PostgresLeaseStore.#ctor(Infrastructure.Persistence.EntityFrameworkCore.AppDbContext)">
            <summary>
            Entity Framework Core / Postgres implementation of <see cref="T:Application.ControlPlane.ILeaseStore"/>.
            Backs the control-plane live-set and watermark tracking with the <c>dc_leases</c> table.
            </summary>
        </member>
    </members>
</doc>
